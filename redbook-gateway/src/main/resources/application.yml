server:
  port: 8000 # 指定启动端口
spring:
  cloud:
    gateway:
      routes:
        - id: auth
          uri: lb://redbook-auth
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=1